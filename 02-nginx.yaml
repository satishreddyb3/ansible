- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx package
    ansible.builtin.package:
      name: nginx
      state: started
